interface:
  display_name: "Goal Mode"
  short_description: "Run bounded Claude Code goal loops"
  default_prompt: "Use $goal-mode to write and run a measurable Claude Code goal."
policy:
  allow_implicit_invocation: false
